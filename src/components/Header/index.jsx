import React from 'react';
import './style.scss';

const Header = () => {
    return (
        <header>
            <button>Users</button>
            <button>Albums</button>
            <button>Photos</button>
            
        </header>
    )
}

export default Header